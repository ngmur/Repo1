package utils;
//Add feature files
//add config reader
//add db utils class which must have a method to establish jdbc
//should have a method to send sql queries and return result
public class ConfigReader {

    //ps -es | grep java
}
